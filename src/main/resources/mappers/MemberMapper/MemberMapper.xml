<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosmo.admin.mapper.MemberMapper">

    <!-- 페이징 및 검색된 사용자 수 조회 -->
    <select id="getTotalCount" resultType="int">
        SELECT COUNT(*)
        FROM users
        <where>
            <if test="filterType != null and filterType != '' and filterType != 'all'">
                <![CDATA[
                ${filterType} LIKE CONCAT('%', #{query}, '%')
                ]]>
            </if>
            <if test="filterType == 'all'">
                (
                id LIKE CONCAT('%', #{query}, '%')
                OR username LIKE CONCAT('%', #{query}, '%')
                OR name LIKE CONCAT('%', #{query}, '%')
                OR email LIKE CONCAT('%', #{query}, '%')
                OR gender LIKE CONCAT('%', #{query}, '%')
                OR role LIKE CONCAT('%', #{query}, '%')
                )
            </if>
        </where>
    </select>

    <!-- 페이징 및 검색된 사용자 목록 조회 -->
    <select id="listMember" parameterType="com.kosmo.admin.dto.MemberPagingDTO" resultType="com.kosmo.admin.dto.MemberDTO">
        SELECT *
        FROM (
        SELECT @rownum := @rownum + 1 AS rn, A.*
        FROM (
        SELECT id, username, name, nickname, role,
        phone_number AS phoneNumber, email, gender,
        terms_agreement AS termsAgreement,
        marketing_agreement AS marketingAgreement,
        created_at AS createdAt, updated_at AS updatedAt
        FROM users
        <where>
            <if test="filterType != null and filterType != '' and filterType != 'all'">
                <![CDATA[
                        ${filterType} LIKE CONCAT('%', #{query}, '%')
                        ]]>
            </if>
            <if test="filterType == 'all'">
                (
                id LIKE CONCAT('%', #{query}, '%')
                OR username LIKE CONCAT('%', #{query}, '%')
                OR name LIKE CONCAT('%', #{query}, '%')
                OR email LIKE CONCAT('%', #{query}, '%')
                OR gender LIKE CONCAT('%', #{query}, '%')
                OR role LIKE CONCAT('%', #{query}, '%')
                )
            </if>
        </where>
        ORDER BY id DESC
        ) A, (SELECT @rownum := 0) R
        ) B
        <![CDATA[WHERE rn > #{start} AND rn <= #{end}]]>
    </select>

    <!-- 유저 삭제 -->
    <delete id="deleteMemberById" parameterType="java.lang.Long">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!-- 유저 수정 -->
    <update id="updateMemberById" parameterType="com.kosmo.admin.dto.MemberDTO">
        UPDATE users
        SET role = #{role}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!--  유저 조회  -->
    <select id="getMemberById" parameterType="com.kosmo.admin.dto.MemberDTO" resultType="com.kosmo.admin.dto.MemberDTO">
        select
        id,
        username,
        nickname,
        phone_number as phoneNumber,
        name,
        email,
        gender,
        role,
        terms_agreement as termsAgreement,
        marketing_agreement as marketingAgreement,
        created_at as createdAt,
        updated_at as updatedAt
        from users
        where id = #{id}
    </select>
</mapper>
