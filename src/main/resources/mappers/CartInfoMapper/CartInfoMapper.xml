<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosmo.cart.mapper.CartInfoMapper">

    <!-- 사용자 정보 조회 -->
    <select id="myinfo" resultType="com.kosmo.cart.dto.MyInfoDTO">
        SELECT name, email, phone_number
        FROM users
        WHERE id = #{userId}
    </select>

</mapper>